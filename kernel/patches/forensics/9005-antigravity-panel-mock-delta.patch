--- a/drivers/gpu/drm/panel/panel-orisetech-otm1911a.c
+++ b/drivers/gpu/drm/panel/panel-orisetech-otm1911a.c
@@ -31,6 +31,10 @@
 	bool prepared;
 };
 
+static bool mock_attach = false;
+module_param_named(mock_attach, mock_attach, bool, 0444);
+MODULE_PARM_DESC(mock_attach, "Mock DSI attach and callbacks for debugging (default: false)");
+
 static const struct drm_display_mode otm1911a_modes[] = {
 	{
 		/* 60 Hz — Nokia 6.1 Plus native panel geometry */
@@ -95,6 +99,8 @@
 	struct mipi_dsi_device *dsi = ctx->dsi;
 	int ret;
 
+	dev_err(&dsi->dev, "ANTIGRAVITY: otm1911a_prepare starting (mock=%d)...\n", mock_attach);
+
 	if (ctx->prepared)
 		return 0;
 
@@ -118,6 +124,12 @@
 		msleep(120); /* Wait for panel initialization */
 	}
 
+	if (mock_attach) {
+		dev_err(&dsi->dev, "ANTIGRAVITY: Bypassing DCS writes in prepare (mock_attach=1)\n");
+		ctx->prepared = true;
+		return 0;
+	}
+
 	ret = mipi_dsi_dcs_exit_sleep_mode(dsi);
 	if (ret < 0)
 		goto vdda_off;
@@ -136,6 +148,12 @@
 	struct otm1911a *ctx = panel_to_otm1911a(panel);
 	int ret;
 
+	if (mock_attach) {
+		dev_err(&ctx->dsi->dev, "ANTIGRAVITY: Bypassing DCS writes in enable (mock_attach=1)\n");
+		backlight_enable(ctx->backlight);
+		return 0;
+	}
+
 	ret = mipi_dsi_dcs_set_display_on(ctx->dsi);
 	if (ret < 0)
 		return ret;
@@ -180,6 +198,13 @@
 
 static int otm1911a_probe(struct mipi_dsi_device *dsi)
 {
+	if (!mock_attach) {
+		dev_err(&dsi->dev, "ANTIGRAVITY: otm1911a_probe early return -ENODEV (mock_attach=0)\n");
+		return -ENODEV;
+	}
+
+	dev_err(&dsi->dev, "ANTIGRAVITY: otm1911a_probe starting (mock_attach=1)...\n");
+
 	struct device *dev = &dsi->dev;
 	struct otm1911a *ctx;
 	int ret;
@@ -216,6 +241,8 @@
 			  MIPI_DSI_MODE_VIDEO_BURST |
 			  MIPI_DSI_MODE_LPM;
 
+	mipi_dsi_set_drvdata(dsi, ctx);
+
 	drm_panel_add(&ctx->panel);
 
 	ret = mipi_dsi_attach(dsi);
